
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: Backend Depl
spec:
  ports:
  - port: 3036
    targetPort: 3036
  selector:
    app: mysql
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: Backend Depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:5.7
        env:

        - name: hhhhh
          
          value: "hhhhhh"
          

        ports:
        - containerPort: 3036
